import math
e = float(input('e = '))
a = float(input('a = '))
b = float(input('b = '))
s = 0 
i = 2
add = 9 * a
factorial = 1 * 2 * 3
for x in range(b - a)
    factorial = 
    while math.fabs(a) >= e : 
        s += add 
        i += 2
        factorial * 
        add = (-1)* math.pow(9 * a) / (factorial)
print (s)
